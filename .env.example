# Postgres (dev)
POSTGRES_USER=teller_user
POSTGRES_PASSWORD=change_me
POSTGRES_DB=teller_storage
POSTGRES_PORT=5432
# Keep localhost for host-run backend now; if backend is containerized later, switch to 'db'
POSTGRES_HOST=localhost

# Production (Render) note:
# Append ?sslmode=require to DATABASE_URL for TLS in production.
