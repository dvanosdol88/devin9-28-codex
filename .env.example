# Postgres (dev/local)
POSTGRES_USER=teller_user
POSTGRES_PASSWORD=change_me
POSTGRES_DB=teller_storage
POSTGRES_PORT=5432
POSTGRES_HOST=localhost

# Production (Render) DATABASE_URL format:
# postgresql://teller_storage_user:PASSWORD@HOST/teller_storage?sslmode=require
#
# To use Render PostgreSQL:
# 1. Get the External Database URL from Render Dashboard
# 2. Append ?sslmode=require to the connection string
# 3. Set as DATABASE_URL environment variable in Render service settings
#
# Note: Do NOT commit the actual DATABASE_URL with credentials to the repo
